# 论文与最终结果一致性核查

核查日期：2026-09-26。对象为当前 `paper/main.tex`、`paper/main.pdf` 及正文实际引用的 24 张图片。以下行号均指本次核查版本的 `main.tex`。本次只形成核查意见，没有改写正文、图片、代码或求解结果。

**结论：四问主要结果及第三、四问的求解关系与此前交付一致，但仍有明确的数值错误、文字与表格矛盾，以及若干模型表达和结果解释不准确之处。最应先改的是第三问链路裕量与瓶颈结论、第一问地形方法、第二问方案结构与最优性表述、第四问资源合计与工作量口径。**

## 1. 核查基准与已经确认一致的内容

本报告以 [正式结果说明](../outputs/REPORT.md)、`outputs/q1`～`outputs/q4`、`outputs/provenance` 和 `outputs/checks` 为基准，对照 `final_code` 的实际实现。仓库内其他运行目录属于不同候选或历史运行，不能混用其指标。

| 项目 | 本次核实的最终结果 | 与论文主要结果的关系 |
| --- | --- | --- |
| Q1 基准 | 18 架次；59.240257802 kWh；累计作业时间 32798.500032 s | 一致 |
| Q2 | 27 架次；15A+6B+6C；6294.864602 s；70.876316700 kWh；80/80 按期；46/46 硬时限 | 摘要、总指标表及逐架次表一致，部分解释文字错误 |
| Q3 | 27 个运输架次+3 个中继架次；14A+7B+6C；6480.604200 s；73.523144786 kWh；77/80 按期；46/46 硬时限 | 主要指标和逐架次表一致 |
| Q3/Q4 关系 | `required_partition_groups=0`；第三问独立优化，冻结后求第四问 | 摘要末段、7.1.4、8.5.2、9.4 已正确说明 |
| Q4，K=2 | 15 个候选；S011 单独成组；缺 B 机体、B 电池各 1；总需求 29；最大 CV=1 | 一致 |
| Q4，K=3 | 25 个候选；S006、S011 各自成组；B/C 机体、电池各缺 1；总需求 31；最大 CV=√2 | 一致 |

采用以下方式交叉检查：

- 程序化逐行比较 Q1 的 18 条、Q2 的 27 条、Q3 的 27 条运输明细，以及 Q1 的载荷矩阵、服务区汇总，共 **834 个数值或标识单元格**；按照论文显示精度比较，未发现不一致。
- 重新汇总 Q2 服务区机型构成、Q3 通信保障时长、Q4 库存需求和工作量 CV。
- 从最终轨迹及源通信模型重新计算配图 CSV 中的 **2690 个采样点、6116 个链路裕量值**；最大差异约 `1.72e-11 dB`，属于浮点误差。
- 提取 PDF 中的图片，确认正文引用的 **24 张图片全部与当前 PNG 的 RGB 像素一致**。下述图文矛盾也存在于当前 PDF，不能通过单纯重新编译解决。

## 2. 应优先修改的确定问题

### 2.1 第三问链路裕量写错，并据此得出了错误依据下的瓶颈结论

**位置：摘要第 104 行；7.6.2 第 1315、1317 行；PDF 第 2、48 页。**

正文写直连、接入、回传链路的裕量中位数分别为 **4.0、5.0、0.7 dB**，并据此认定“瓶颈在回传链路”。当前图 `q3_link_margin.png` 上的标注以及 `pic/问题3/链路裕量采样.csv` 实际为：

| 链路 | 样本数 | 中位数精确值 / dB | 图中显示 / dB |
| --- | ---: | ---: | ---: |
| 直连 | 2690 | 4.301887928 | 4.3 |
| 中继接入 | 1713 | 4.519170516 | 4.5 |
| 中继回传 | 1713 | 9.848345330 | 9.8 |

上述数值已从最终方案重新计算核实。因此 **0.7 dB 不是当前方案的结果，“回传几乎吃掉全部余量”的分析应删除或重做**，不能只换一个数字而保留原结论。

建议改为：

> 三类链路的抽样裕量中位数分别为 4.3、4.5、9.8 dB。当前回传链路的中位裕量高于接入链路；这些抽样分布用于描述链路特征，不能仅凭不同样本集合的中位数认定全局瓶颈。连续通信可行性由完整区间证书保证。

补充：图中接入链路存在负裕量样本，不等于已认证方案通信中断。绘图脚本会在该运输架次对应中继的整个服务窗内计算接入裕量，包括运输机当时可以直连、并不需要中继接入的时刻。建议在图注说明样本集合。

### 2.2 地形方法仍写成旧的等距采样、双线性插值，与代码和结果不一致

**位置：摘要第 78 行；4.2 第 270～280 行；6.2.1 第 794 行；PDF 第 10 页。**

4.2 将正式方法描述为“不超过 30 m 的等间距采样+双线性内插”。实际 `final_code/problem1/solver.py:180` 的 `sample_leg` 使用：

1. 经纬度坐标线段与栅格网格的交点；
2. 遍历线段碰到的全部闭像元，包含压边与过角；
3. 直接取这些像元的最高 DEM 值，再加 50 m。

四问共用这一口径。论文 Q1 表中的 S007 巡航海拔 511.97 m、S011 的 295.36 m，也已经是新口径结果。

建议把 4.2 的正式公式统一为：

`H_max = max{DEM(c): c 为航段接触的闭像元}`。

旧的 30 m 采样法可留作对照，但必须明确标为旧方法。

此外，“两种方法给出的净空都满足 50 m”不成立。S007 旧巡航海拔 501.83 m，相对完整像元最高高程 461.97 m，仅约 **39.86 m** 净空。它只是在遗漏山脊的旧采样集上显示 50 m，不能称为符合题面完整像元口径。

### 2.3 第二问有三处明确的架次结构错误

**位置：6.5.2 第 958 行；6.5.3 第 1009 行；PDF 第 33～34 页。**

- “最终方案共出动 **28** 个架次”应为 **27**。
- S001“**2 架 C 型加 4 架 A 型**”应为 **1 个 C 型架次加 5 个 A 型架次**。对应 C 型 P00341；A 型 P00128、P00338、P00032、P00134、P00131。
- “S009 至 S015 各用 1 个架次，机型统一为 B 型”属于第一问的结构，不能用于第二问。第二问实际为：

| 服务区 | 访问该区的架次数 | 机型构成 |
| --- | ---: | --- |
| S009 | 2 | A+B |
| S010 | 2 | A+A |
| S011 | 2 | A+A |
| S012 | 1 | B |
| S013 | 2 | A+B |
| S014 | 1 | B |
| S015 | 1 | B |

这里按“访问该服务区”计数，多点架次会同时计入多个服务区，不能再把此列相加当全网架次数。

依据：[Q2 架次原始表](../outputs/q2/sorties_audit.csv)。论文逐架次明细本身正确，应据此修正文段。

### 2.4 第四问“全局峰值”合计错误，“分区开销”混合了两种 K

**位置：8.4.2 第 1597～1613 行。**

表内八类未分区峰值为 `4,2,2,6,4,4,2,3`，合计应为 **27**，不是 26。

因此：

| 指标 | K=2 | K=3 |
| --- | ---: | ---: |
| 未分区资源峰值合计 | 27 | 27 |
| 分区后总需求 | 29 | 31 |
| 分区开销 | **2** | **4** |
| 库存缺口 | 2 | 4 |

现表只有一列“分区开销”，C 型机体、电池均写 1，这只适用于 K=3；K=2 时这两项都是 0。应拆为两列，或明确该列仅表示 K=3。

依据：[库存比较](../outputs/q4/inventory_comparison.csv)。库存缺口与分区开销在本方案中数值相同，但定义不同，不能据此混用。

### 2.5 第二问“给定模式库内最优”仍然说得过强

**位置：6.3.3 第 891 行；9.3 第 1689 行；PDF 第 64 页。**

论文一处称“限定时间内的字典序最优解”，另一处称“给定模式库内的字典序最优”。但最终 `validation.json` 中：

- 完工时间、能耗、架次数阶段都是 `FEASIBLE`；
- 只有迟到量阶段达到 `OPTIMAL=0`；
- `restricted_lexicographic_optimal=false`；
- `global_optimal=false`。

所以 **即使限定为现有模式库，也没有证明完整四级字典序最优**。

建议统一为：

> 在有限模式库和给定时间预算下，按字典序准则筛选得到的最好已知可行解；迟到量为 0，其他阶段尚未证明最优。

6.6.2、7.7.2 中的 5.4%、2.6%、8.4%、7.4% 可保留为对应受限整数阶段的“上下界相对差距”，不宜写成已经确认存在同等幅度的改进空间，更不能称为原题全局最优间隙。

### 2.6 第二问的两处数学表达与实际实现不同

**位置：6.3.2 第 865、870 行；6.4.2 第 901 行。**

**完工时间约束缺少选择条件。** 论文写对全部候选 `p∈L` 都有 `Cmax≥s_p+ceil(d_p)`。这样未选用的长模式也会抬高完工时间。代码用可选区间，并令未选模式的结束变量为 0。

建议写为：

`x_p=1 ⇒ Cmax≥s_p+ceil(d_p)`。

**电池占用长度的取整位置不同。** 论文用 `ceil(d_p+c_p)`，`master.py:91～96` 实际用 `ceil(d_p)+ceil(c_p)`。两者可能相差 1 s；两种形式均可以保守，但不是同一个整数模型。论文应按实际实现修改。

### 2.7 第三问通信保障占比被解释成了精确无线状态占比

**位置：摘要第 104 行；7.6.2 第 1306 行。**

23301.704512 s、12720.316305 s 及 64.7%、35.3% 的数值确实能由通信审计表汇总得到。但它们表示**累计运输架次上的区间保障方式**，不是单一墙钟时间，也不一定等于直连优先策略下逐瞬间的实际通信模式占比。

当整段直连无法取得证书而中继能覆盖整段时，审计将整段标为“中继”；其中部分瞬间仍可能直连。当前表中有 **19 个标为中继的区间，其保存的中点直连损耗已经不超过门限**，足以说明这一区别。

建议改为“直连认证区间占比”“中继保障预留区间占比”，并删除由这些分段直接推断“实际无线切换频繁”的断言。

另外，7.6.3 的供需图按 120 s 分桶，需求统计的是桶内出现过需求的不同运输架次数，供给统计的是服务窗与桶相交的中继数。它不能单独证明每一时刻、每一架运输机都有合法接入链路。此次从正式区间另算的中继保障预留并发峰值也为 6，因此数字 6 本身不是错误；应把其依据写清楚，连续覆盖结论仍引用区间认证。

### 2.8 第四问工作量名称和 CV 描述不准确

**位置：8.2.3 第 1540 行附近；8.4.3 第 1639 行；时间—能耗图及其图注。**

第一，`transport_flight_time_s` 虽然字段名叫“飞行时间”，实际由 `problem4/solver.py:392` 计算为“返航时刻－起飞时刻”，**包含服务区交接时间**。本方案：

- 起飞至返航累计时长：36022.020817 s；
- 纯爬升、巡航、下降累计时长：29014.020817 s；
- 交接累计时长：7008 s。

例如 S011 的 P00018：纯飞行 559.630805 s，但 Q4 记录为 799.630805 s，包含 240 s 交接。

这不是论文表中数值抄错，而是现有结果字段命名与物理含义不完全一致。若保留当前结果，建议统一称为“运输空中作业时间（含投送交接）”。若改用纯飞行时间作为评价指标，则属于改变评价口径，应重新核算，不能只替换标签。

第二，“四项工作量的变异系数在 K=2 时为 1.00，K=3 时为 1.41”不准确；这两个数只是四项中的**最大值**：

| 工作量维度 | K=2 CV | K=3 CV |
| --- | ---: | ---: |
| 运输空中作业时间（含交接） | 0.955603 | 1.303507 |
| 中继服务时间 | 1.000000 | 1.414214 |
| 运输架次数 | 0.925926 | 1.257079 |
| 货箱数 | 0.925000 | 1.176462 |

依据：[工作量比较](../outputs/q4/workload_comparison.csv)。建议改为“四项工作量 CV 的最大值分别为 1.00、1.41”，并补充各维度表。

### 2.9 第四问“把 S011 并回主体组更划算”的建议改变了题目条件

**位置：8.4.2 第 1629 行；9.5 第 1708 行。**

当前 K=2 方案直接合并 S011 后只剩 1 组；K=3 直接合并后只剩 2 组，均不再满足原来的组数要求。若必须维持原 K，就需要拆分其他原子单元组合，再重新核算资源；不能仅凭“省下一套机电池”断言是同问题下更优方案。

此外，“主体组峰值略升”也不符合当前数据：主体组的八类资源峰值已经等于未分区峰值，合并 S011 并不使这些峰值再上升。

建议改为：

> 若管理上允许减少独立任务组数，跨组合并可以恢复资源共享；在题目规定的 K=2、3 下，当前方案已达到冻结任务图中的最小库存缺口。继续降低缺口需要改变分组要求，或在第三问阶段重新优化任务方案。

### 2.10 第四问原子单元的合并原因与集合写法有误

**位置：8.1.2 第 1498 行；8.4.1 第 1583 行。**

- 8 个服务区的单元不是全部由 RU-002 直接保障。RU-002 直接涉及 S002、S003、S005、S007、S008、S009、S015，共 7 个区；RU-003 涉及 S004、S008，通过共同的 S008 把 S004 纳入传递闭包。表中原因应改为“RU-002、RU-003 经 S008 连通形成传递闭包”。
- `S010～S014` 包含 S011，写法会与“S011 独立成单元”冲突。应明确列为 **S010、S012、S013、S014**。

原子单元表的集合和实际分区本身正确，错误在解释和简写。

### 2.11 第一问把累计时间增量写成地面时间增量，且把临界余量误写成 20%

**位置：摘要第 84 行；5.4.2 第 552 行；5.6.1 第 690 行。**

把 S008 的 1 个 C 型架次改为 2 个 B 型架次后：

| 时间增量 | 数值 / s |
| --- | ---: |
| 累计作业时间 | 1668.020335 |
| 地面准备、装载与交接 | **390.000000** |
| 纯飞行时间 | **1278.020335** |

因此“多花 1668 s 地面作业时间”“真正被牺牲的是地面作业时间”都不准确，大部分增量其实来自多飞一次往返。应改为“累计作业时间增加 1668.0 s，其中飞行增加 1278.0 s，地面作业增加 390.0 s”。

“余量一旦超过 20% 就触发裂变”也不成立。当前方案最低返航 SOC 为 **22.789267%**，所以原方案至少在余量提高到这一数值前仍可行。25% 是已计算到方案变化的采样档位，不是证明得到的精确临界点。建议写为“20% 档位方案稳定，在 25% 档位观察到 S004 必须拆分”。

### 2.12 第二问与第三问最终方案来自不同运行，扩库效果的解释缺少依据

**位置：6.5.1 第 924、946 行；7.3.1 第 1188 行；7.6.1 第 1296 行。**

最终来源为：

| 问题 | 最终来源及搜索记录 |
| --- | --- |
| Q2 | `review_search`；341→601 模式；每轮每问总预算参数 240 s；种子 31、32 |
| Q3/Q4 | 原 `run01` 的第三问方案，经 `review_refined` 连续 LP 改进；Q3 465 模式；父运行预算参数 120 s；最终种子 18 |

二、三问使用共同的模式生成机制，但**最后选中的两个解并不来自同一份、同一轮模式库快照**。建议加一段明确来源，避免把 Q2 的 341→601 扩展过程与 Q3 的 465 模式混成一次实验。

第 946 行称扩展模式“对第三问更有价值、对第二问只是重复已有选项”，没有实验支持：在该 Q2 来源运行中，两轮 Q3 联合完工时间分别约 7676.44 s、10527.04 s，均未取代最终 6480.60 s 方案。新增模式也经过去重，不能称为已有模式的重复。应改为“扩库在这次限时搜索中未改善所选第二问指标，不能据此证明初始库完备或新增列无价值”。

两轮 Q2 的主要指标相同，也不足以单凭 `feedback_history.json` 证明全部箱组、资源编号和时刻“逐位相同”；无完整逐字段比对证据时建议只写“主要指标相同”。

第 1097 行“在数十秒内给出本方案”没有对应的完整运行耗时证据，而且所选运行的每轮每问 CP-SAT 总预算参数为 240 s，另有建库、通信认证、精修等耗时。建议改为“在给定时间预算内”，或补充真实总耗时与运行环境。

### 2.13 第三问部分模型叙述缺少代码实际采用的关键项

| 位置 | 论文问题 | 与实现一致的修改 |
| --- | --- | --- |
| 7.3.1，第 1188 行 | 通信需求被说成“只取决于航线几何，与具体箱组无关” | 缓存键实际包含**访问序、机型、各服务区箱数**；速度、装载和交接时长会改变相对通信需求窗。只是不依赖具有相同这些属性的箱号和质量差异。见 `patterns.py:26` |
| 7.4.1，第 1212 行 | 预处理的相对需求时间窗直接与绝对服务窗比较，没有定义转换 | 明确 `t_start=s_p+delta_start`、`t_end=s_p+delta_end`，或者直接在覆盖约束中写出这两个和 |
| 7.5.2，第 1241 行 | LP 被概括为“只让开始时刻连续变化” | 除运输开始时刻，还优化**中继服务开始和结束时刻**。否则中继服务时长及能耗不能按论文所列方式减少。见 `refine.py:22～24` |
| 7.1.3，第 1149 行 | 悬停能耗只写功率乘服务时长 | 实际还包括 **30 s 建链期间**的悬停与通信耗能：`(P_hover+P_comm)×(30+服务时长)/3600`。3 个任务合计这部分建链能耗为 0.0275 kWh，已包含在正式结果中 |
| 7.3.2，第 1193 行 | “四项检查都不通过的点位剔除” | 应为“**任一项不通过即剔除**”；四项须全部满足 |

这些修改是补全论文模型，不要求改变当前可行结果。

### 2.14 第三问部分因果解释超出了结果能够支持的范围

**位置：7.6.1 第 1296 行；7.6.3 第 1370 行；7.6.4 第 1375、1427 行。**

- 运输能耗从 70.876 降到 70.059 kWh，并不意味着“运输方案没有变差”：完工时间由 6294.86 升至 6480.60 s，按期率由 100% 降至 96.25%，加权迟到由 0 升至 32999.17。应表述为不同指标之间的取舍。
- 本方案 P00018 与 RU-003 的返航时刻均为 6480.604200 s。第三问与第二问之间约 185.74 s 的差异不能简单分解成“中继返航增加了多少”，因为运输路线和时刻也变了。当前联合完成时间与运输完成时间之差为 0。
- R02 在 RU-001 后换用 RE-03 执行 RU-003，并非等待原 RE-02 充满。RU-003 准备开始于 4176.668 s，而 RE-02 到 4612.563 s 才充满。因此“中间留出了充电时间”容易误导，应说明机体周转、换能源组件及服务窗安排。
- 中继只须覆盖已分配的通信需求窗，不必“持续悬停到最后一个受保障架次飞完”。例如 RU-003 在 5841.755 s 结束服务，而其保障的 P00009 到 6344.447 s 才返航；后续阶段可以直连。
- 两点回路从 3 条减少到 1 条是观察到的现象。由于两个最终方案来自不同限时运行、模式库快照和目标权衡，仅据两个方案不能断言这是通信约束的“直接后果”，更不能证明“中继不会成为瓶颈”。应注明为可能的解释，或增加控制其他因素的对照实验。

## 3. 其他应同步修正或补充的内容

| 位置 | 问题 | 建议 |
| --- | --- | --- |
| 摘要第 73 行、关键词第 20 行 | 仍称“给出问题一的完整结果”，关键词也集中于 Q1 | 改为覆盖四问的说明，补入运输与中继联合调度、连续通信、冻结分区等关键词 |
| 假设引言第 161 行 | 写“以下 7 条”，实际列了 10 条 | 改数量，或删除具体条数 |
| 1.1 第 135 行 | “不能跨服务区合并”作为整篇的一般限制 | 明确仅限第一问，第二、三问允许多服务区组批 |
| 5.2.3 第 461 行 | A 型空载单位距离电耗 0.18 Wh/m 被称为最低 | B 型为约 0.143 Wh/m，更低；改为准确的机型比较 |
| 5.5.2 第 648 行 | S001 被称为距基地最近 | 最近是 S011，2817.15 m；S001 为 3063.41 m，可称较近 |
| 5.6.1 | 正文仅展示 10%～30% 档位 | 不构成数值矛盾，但完整结果还包含 40%、50% 时部分服务区不可行，建议补表并列出不可行服务区，避免遗漏已有敏感性结论 |
| 6.1.3 第 767 行 | 用“最后一件物资送到”解释最晚返航目标 | 两者不是同一时刻。应写明采用“全部运输架次返抵基地”的完成时间，并说明这是公开的目标选择 |
| 摘要 Q2 段、6.5.3 第 1007 行 | Q1 累计作业时间与 Q2 墙钟完工时间易被直接比较 | Q1 的 32798.5 s 不是排程 makespan。若称 27 架次优于 18 架次排程，需展示同一资源、时限条件下的 18 架次对照结果 |
| 6.3.2 第 877 行 | “避免硬约束箱被重复计入软目标”不准确 | 15 个首批饮用水箱仍有硬截止与软期望时间两个指标；只排除医疗箱的软迟到。当前代码虽对医疗箱也建迟到项，但硬时限使其恒为 0，与非医疗集合统计等价 |
| 7.1.2 第 1136 行 | 接入链路称门限由运输机发射方向决定 | 两端发射功率同为 20 dBm，两方向增益之和相同，两个方向预算相等；116 dB 数值正确 |
| 7.1.2 第 1142 行 | “第 5 章的图”及“实测裕量” | 图在第 7 章；裕量由模型计算，不是现场实测 |
| 7.6.2 第 1317 行 | 回传端被称为“发射功率和天线增益都最高” | 回传端发射功率 19 dBm，低于运输及接入端的 20 dBm，也低于网关的 27 dBm；应区分发射功率、单端增益和双向预算总量 |
| 7.2.2 第 1172 行 | 垂直运动时称只需检查一条竖直线 | 退化为点的是**移动端的地面投影**，端点到网关的 LOS 水平投影仍是一条线段；实际代码遍历这条线段触及的全部 DEM 像元 |
| 7.3.2 第 1191 行 | 3 个参考中继位置被称为“附件给出” | 代码中为 `REFERENCE_RELAY_POSITIONS` 固定候选，基础工作簿中未找到这些参考位置的明确给定依据。需补来源；否则写为自行选取的参考候选，区别于附件中的服务区坐标 |
| 7.6.3 及供需图注 | “沿用附件无限并发假设”来源表述过强 | 题面未提供接入容量上限；应写“题面未给定容量上限，本文不另加同一中继并发接入数约束”，明确这是实现口径 |
| 摘要 Q4 段、8.2.2 第 1531 行 | 扫描线总复杂度称为线性 | 当前 `_peak` 先排序事件，总复杂度为 O(n log n)，排序后的扫描为 O(n) |
| 9.1 第 1672 行 | “毫秒级”求得 Q1 最优 | 未在正式结果中找到对应运行计时证据；保留精确 DP 结论，删除未经计时支持的速度表述或补测试环境和实测耗时 |
| 9.1 第 1673 行 | κ=0.8“结果不变化” | 不变的是架次和组批结构；总能耗明显变化，应明确指标 |
| Q2/Q3 明细表 | 开始与起飞时刻取整，返航保留 1 位小数 | 加脚注说明仅为显示取整，执行与资源审计以原始 CSV 连续时刻为准，避免读者把显示后的首尾差视为资源重叠 |
| 摘要第 104 行 | 在 LaTeX 中使用 `**通信中断...**` | 改为 `\textbf{...}`，避免 PDF 原样显示星号 |

## 4. 不应被误判为矛盾的内容

1. **5589 与 11913 个通信区间均有依据。** `q3/screening.json` 保存的生成时审计为 5589，论文写这一数值正确；`checks/audit.json` 从冻结保障窗重新细分独立复核得到 11913。划分事件点不同，数量不同并不意味着漏审。建议同时标明两种审计及零未认证区间结论。
2. **6482、6481.630805 与 6480.604200 s 是不同阶段。** 分别对应整数模型指标、已有连续回放方案、LP 精修后最终结果，标清阶段即可，不应全部替换成同一数字。
3. **第三问运输能耗较低而总能耗较高可以成立。** 运输 70.058566646 kWh，加中继 3.464578140 kWh，合计 73.523144786 kWh。分别保留三位小数后 70.059+3.465 与总数末位差 0.001，是独立四舍五入，不是算错。
4. **“合法分区”与“库存不足”不矛盾。** 两种 K 都存在合法任务归属，但分别缺 2、4 件指定类型资源，论文已作区分。
5. **第三问独立优化并不等于先锁定第二问再补中继。** 当前论文 7.1.4 的解释正确：第三问内部运输与中继联合重选；第三、四问之间没有采用分区反馈择优。
6. **Q4 资源编号可以在组内重配。** 冻结的是任务、时间与保障关系，不要求把第三问跨时段复用的同一实体编号同时划给多个独立组；需求按每组同类资源峰值计算。

## 5. 建议修改顺序

1. 先修正 2.1～2.6 的图文、数值、方法和最优性冲突。
2. 再统一通信预留时长、空中作业时间、CV、组数约束及方案来源口径。
3. 按实际代码补全相对时间窗、建链能耗、LP 变量和缓存键说明。
4. 最后统一摘要、结论、图注和阶段数值，重新编译 PDF。数值基准与正确的逐架次表无需因这些文字问题重新优化。

本次没有联网核验参考文献、灾情报道或文献对具体方法的支撑范围；这些属于另一项文献核查。本报告的确定结论均来自本地题面、源数据、现有代码与保存的结果。

## 6. 核查版本

- `main.tex` SHA-256：`eca8d94d15122789a94101889fd744b80b24fb8ffe5c30be940c602f5a248ec9`
- `main.pdf` SHA-256：`be9eddb14243975eafd4c747cb11ec411cb0cbec9fc53825d9b1313fbc088b75`

`paper/问题一论文核查报告.md`、`paper/问题二论文核查报告.md` 是历史核查记录，其中部分数字来自旧版本方案，不能把那些历史数字重新覆盖到本次论文。后续修改应以上述最终基准文件为准。
